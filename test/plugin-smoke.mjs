import assert from 'node:assert/strict'; import * as plugin from '../index.js'; assert.equal('default' in plugin, false); assert.equal(plugin.name, 'dsh-attestation-proof'); assert.deepEqual(plugin.inject, ['tools']); assert.deepEqual(plugin.createDefinitions().map(x => x.name), ['dsh_attestation_proof_inspect', 'dsh_attestation_proof_verify']); process.stdout.write(`${JSON.stringify({ ok: true, namespaceLoaderSafe: true, tools: 2 })}\n`)
